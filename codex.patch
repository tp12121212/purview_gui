 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/app.py b/app.py
index 2925aee5c42d8b3973e8402b4bcee3a45b534811..64ca8a423219a777ad648bb7d0275466669f0e1a 100644
--- a/app.py
+++ b/app.py
@@ -27,50 +27,51 @@ app = Flask(__name__)
 CORS(app, resources={
     r"/*": {
         "origins": [
             "http://127.0.0.1:5500",
             "http://localhost:5500",
             "null",
             "file://",
         ]
     }
 })
 
 # Allow large images while keeping a sane cap to avoid DoS warnings.
 Image.MAX_IMAGE_PIXELS = 200_000_000
 warnings.filterwarnings(
     "ignore",
     category=Image.DecompressionBombWarning,
 )
 
 # ======================================================
 # Configuration
 # ======================================================
 
 DEFAULT_LEXICON_PATH = "lexicon_latest.csv"
 REGEX_PATTERNS_PATH = "regex_patterns.json"
 PROGRESS_DIR = Path(tempfile.gettempdir()) / "purview_scan_progress"
+MAX_SIT_FILES = 50
 
 SUPPORTED_EXTENSIONS = {
     ".pdf", ".docx", ".xlsx",
     ".jpg", ".jpeg", ".png", ".tif", ".tiff",
     ".eml", ".msg"
 }
 
 app.config["MAX_CONTENT_LENGTH"] = 500 * 1024 * 1024  # 500MB
 MAX_NESTED_DEPTH = 2
 
 # ======================================================
 # Lexicon loading (single-word only)
 # ======================================================
 
 def load_keywords(lexicon_path: str = DEFAULT_LEXICON_PATH):
     keywords = []
 
     with open(lexicon_path, newline="", encoding="utf-8") as f:
         reader = csv.DictReader(f)
         for row in reader:
             value = (
                 row.get("keyword")
                 or row.get("phrase")
                 or row.get("value")
             )
@@ -316,85 +317,94 @@ def nearest_regex_info(start, end, regex_hits):
     closest = None
     min_distance = None
 
     for r in regex_hits:
         if r["end"] < start:
             d = start - r["end"]
         elif r["start"] > end:
             d = r["start"] - end
         else:
             d = 0
 
         if min_distance is None or d < min_distance:
             min_distance = d
             closest = r["name"]
 
     return closest, min_distance
 
 # ======================================================
 # API endpoint
 # ======================================================
 
 @app.route("/scan", methods=["POST"])
 def scan():
     lexicon_path = request.form.get("lexicon_path", DEFAULT_LEXICON_PATH)
     regex_path = request.form.get("regex_path", REGEX_PATTERNS_PATH)
+    scenario_mode = request.form.get("scenario_mode", "sit").strip().lower()
     scan_id = request.form.get("scan_id")
     scan_path = request.form.get("path")
     recursive = request.form.get("recursive", "true").lower() == "true"
     log_path = request.form.get("log_path")
     log_to_stdout = request.form.get("log_stdout", "true").lower() == "true"
     debug_text = request.form.get("debug_text", "false").lower() == "true"
     debug_text_path = request.form.get("debug_text_path")
     def normalize_output_path(value, default):
         if value is None:
             return default
         value = value.strip()
         if not value:
             return default
         if value.lower() in {"none", "null", "false"}:
             return None
         return value
 
     regex_output_path = normalize_output_path(
         request.form.get("regex_output_path"),
         "regex_matches.csv",
     )
     keyword_output_path = normalize_output_path(
         request.form.get("keyword_output_path"),
         "keyword_matches.csv",
     )
+    if scenario_mode == "scan_only":
+        keyword_output_path = None
     allowed_exts = parse_file_types(request.form.get("file_types"))
     output_path = request.form.get("output_path")
     batch_size = int(request.form.get("batch_size", "500"))
     return_limit = int(request.form.get("return_limit", "0"))
 
     if not lexicon_path:
         lexicon_path = DEFAULT_LEXICON_PATH
     if not regex_path:
         regex_path = REGEX_PATTERNS_PATH
 
+    if scenario_mode not in {"sit", "scan_only"}:
+        return jsonify({
+            "success": False,
+            "error": "scenario_mode must be 'sit' or 'scan_only'"
+        }), 400
+
     progress_path = None
     if scan_id and re.fullmatch(r"[A-Za-z0-9_-]{6,64}", scan_id):
         PROGRESS_DIR.mkdir(parents=True, exist_ok=True)
         progress_path = PROGRESS_DIR / f"{scan_id}.json"
 
     def write_progress(payload):
         if not progress_path:
             return
         payload["scan_id"] = scan_id
         with open(progress_path, "w", encoding="utf-8") as f:
             json.dump(payload, f)
 
     if progress_path:
         write_progress({
             "status": "starting",
             "current": 0,
             "total": 0,
             "percent": 0,
             "current_document": "Listing files..."
         })
 
     log_file = None
     if log_path:
         log_file = open(log_path, "a", encoding="utf-8")
 
@@ -436,130 +446,185 @@ def scan():
     regex_upload_path = save_uploaded_file(uploaded_regex, ".json")
     if lexicon_upload_path:
         lexicon_path = lexicon_upload_path
     if regex_upload_path:
         regex_path = regex_upload_path
 
     scan_items = []
     listing_count = 0
     for scan_item in iter_scan_items(
         request.files.getlist("files"),
         scan_path,
         recursive,
         allowed_exts,
     ):
         scan_items.append(scan_item)
         listing_count += 1
         if scan_path and listing_count % 50 == 0:
             write_progress({
                 "status": "listing",
                 "current": listing_count,
                 "total": 0,
                 "percent": 0,
                 "current_document": f"Found {listing_count} files..."
             })
     total_documents = listing_count
+    if scenario_mode == "sit" and (total_documents == 0 or total_documents > MAX_SIT_FILES):
+        write_progress({
+            "status": "error",
+            "error": f"Scenario 1 requires 1-{MAX_SIT_FILES} files. Found {total_documents}."
+        })
+        cleanup_temp_files()
+        if log_file:
+            log_file.close()
+        if debug_file:
+            debug_file.close()
+        return jsonify({
+            "success": False,
+            "error": f"Scenario 1 requires 1-{MAX_SIT_FILES} files. Found {total_documents}."
+        }), 400
     write_progress({
         "status": "running" if total_documents > 0 else "complete",
         "current": 0,
         "total": total_documents,
         "percent": 0 if total_documents > 0 else 100,
         "current_document": "" if total_documents > 0 else "No files found"
     })
 
     regex_csv_file = None
     regex_csv_writer = None
     if regex_output_path:
         regex_csv_file = open(regex_output_path, "a", encoding="utf-8", newline="")
         regex_csv_writer = csv.DictWriter(
             regex_csv_file,
             fieldnames=[
                 "regex_name",
                 "document",
                 "position",
                 "value",
                 "context",
             ],
         )
         if os.path.getsize(regex_output_path) == 0:
             regex_csv_writer.writeheader()
 
     keyword_csv_file = None
     keyword_csv_writer = None
-    if keyword_output_path:
+    if keyword_output_path and scenario_mode == "sit":
         keyword_csv_file = open(keyword_output_path, "a", encoding="utf-8", newline="")
         keyword_csv_writer = csv.DictWriter(
             keyword_csv_file,
             fieldnames=[
                 "phrase",
                 "document",
                 "position",
                 "context",
                 "nearest_regex",
                 "nearest_regex_distance",
             ],
         )
         if os.path.getsize(keyword_output_path) == 0:
             keyword_csv_writer.writeheader()
 
     def log(line: str):
         if log_to_stdout:
             print(line)
         if log_file:
             log_file.write(line + "\n")
             log_file.flush()
 
     def log_debug_text(line: str):
         if debug_file:
             debug_file.write(line + "\n")
             debug_file.flush()
 
     if not os.path.isfile(regex_path):
         write_progress({
             "status": "error",
             "error": f"regex_path not found: {regex_path}"
         })
         cleanup_temp_files()
         if log_file:
             log_file.close()
         if debug_file:
             debug_file.close()
         if regex_csv_file:
             regex_csv_file.close()
         if keyword_csv_file:
             keyword_csv_file.close()
         return jsonify({
             "success": False,
             "error": f"regex_path not found: {regex_path}"
         }), 400
 
-    keywords = load_keywords(lexicon_path)
-    keyword_set = set(keywords)
+    keywords = []
+    keyword_set = set()
+    if scenario_mode == "sit":
+        keywords = load_keywords(lexicon_path)
+        keyword_set = set(keywords)
     regex_patterns = load_regex_patterns(regex_path)
     # Keep debug_text focused on extracted document text only.
 
     warned_regex_csv_disabled = False
+    sit_outputs = None
+    if scenario_mode == "sit":
+        sit_outputs = {
+            "sit_name": request.form.get("sit_name", "").strip() or "Custom SIT",
+            "sit_description": request.form.get("sit_description", "").strip(),
+            "sit_publisher": request.form.get("sit_publisher", "").strip() or "Purview Custom",
+            "rule_pack_name": request.form.get("rule_pack_name", "").strip() or "CustomRulePack",
+        }
+        regex_xml_entries = []
+        for r in regex_patterns:
+            regex_name = html_lib.escape(r.get("name", "Regex"))
+            regex_pattern = html_lib.escape(r.get("pattern", ""))
+            regex_xml_entries.append(
+                f'    <Regex name="{regex_name}" pattern="{regex_pattern}" />'
+            )
+        regex_xml = "\n".join(regex_xml_entries) if regex_xml_entries else "    <!-- No regex patterns found -->"
+        sit_outputs["rule_pack_xml"] = (
+            "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n"
+            "<RulePack>\n"
+            f"  <RulePackInfo name=\"{html_lib.escape(sit_outputs['rule_pack_name'])}\" "
+            f"publisher=\"{html_lib.escape(sit_outputs['sit_publisher'])}\" />\n"
+            "  <SensitiveInformationTypes>\n"
+            f"    <SensitiveInformationType name=\"{html_lib.escape(sit_outputs['sit_name'])}\">\n"
+            f"      <Description>{html_lib.escape(sit_outputs['sit_description'])}</Description>\n"
+            "      <Regexes>\n"
+            f"{regex_xml}\n"
+            "      </Regexes>\n"
+            "    </SensitiveInformationType>\n"
+            "  </SensitiveInformationTypes>\n"
+            "</RulePack>\n"
+        )
+        sit_outputs["powershell_script"] = (
+            "$rulePackPath = \"./"
+            + sit_outputs["rule_pack_name"]
+            + ".xml\"\n"
+            "New-DlpSensitiveInformationTypeRulePackage -FileData "
+            "(Get-Content -Path $rulePackPath -Encoding Byte -ReadCount 0)\n"
+        )
 
     output_file = None
     if output_path:
         output_file = open(output_path, "a", encoding="utf-8")
 
     results = []
     batch_results = []
     errors = []
     documents_scanned = 0
     total_matches = 0
 
     def record_match(item):
         nonlocal total_matches
         total_matches += 1
 
         if output_file:
             batch_results.append(item)
             if len(batch_results) >= batch_size:
                 output_file.write("\n".join(json.dumps(r) for r in batch_results))
                 output_file.write("\n")
                 output_file.flush()
                 batch_results.clear()
 
         if not output_file or (return_limit > 0 and len(results) < return_limit):
             results.append(item)
@@ -586,51 +651,51 @@ def scan():
             else:
                 text = extract_text_from_file_path(item)
 
             if debug_text:
                 log_debug_text(f"extracted_text_begin: {document}")
                 log_debug_text(text)
                 log_debug_text(f"extracted_text_end: {document}")
 
             # -------------------------
             # Regex hits
             # -------------------------
             regex_hits = []
             for r in regex_patterns:
                 for m in re.finditer(r["pattern"], text, re.IGNORECASE | re.DOTALL):
                     regex_hits.append({
                         "name": r["name"],
                         "start": m.start(),
                         "end": m.end(),
                         "value": m.group(0)
                     })
 
             # -------------------------
             # Keyword hits (strict), only after a regex match exists
             # -------------------------
             keyword_hits = []
-            if regex_hits:
+            if regex_hits and scenario_mode == "sit":
                 token_iter = list(re.finditer(r"[A-Za-z0-9]+", text))
                 for idx in range(len(token_iter) - 1):
                     first = token_iter[idx]
                     second = token_iter[idx + 1]
                     between = text[first.end():second.start()]
                     if between != " ":
                         continue
                     w1 = first.group(0).lower()
                     w2 = second.group(0).lower()
                     if w1 in keyword_set and w2 in keyword_set:
                         keyword_hits.append({
                             "phrase": f"{w1} {w2}",
                             "start": first.start(),
                             "end": second.end()
                         })
 
             logged_file = False
             if regex_hits and not regex_csv_writer and not warned_regex_csv_disabled:
                 log("warning: regex_output_path disabled; regex CSV not written")
                 warned_regex_csv_disabled = True
             for r in regex_hits:
                 if not logged_file:
                     log(f"matched_file: {document}")
                     logged_file = True
                 log(
@@ -700,50 +765,52 @@ def scan():
         output_file.flush()
         batch_results.clear()
 
     if output_file:
         output_file.close()
 
     if log_file:
         log_file.close()
     if debug_file:
         debug_file.close()
     if regex_csv_file:
         regex_csv_file.close()
     if keyword_csv_file:
         keyword_csv_file.close()
     cleanup_temp_files()
     write_progress({
         "status": "complete",
         "current": documents_scanned,
         "total": total_documents,
         "percent": 100,
         "current_document": ""
     })
 
     return jsonify({
         "success": True,
+        "scenario_mode": scenario_mode,
+        "sit_outputs": sit_outputs,
         "documents_scanned": documents_scanned,
         "total_matches": total_matches,
         "matches": results,
         "errors": errors
     })
 
 
 @app.route("/health", methods=["GET"])
 def health():
     return jsonify({
         "status": "ok",
         "ocr_available": True
     })
 
 
 @app.route("/", methods=["GET"])
 def index():
     return send_from_directory(".", "index.html")
 
 
 @app.route("/index.html", methods=["GET"])
 def index_html():
     return send_from_directory(".", "index.html")
 
 @app.route("/regex-files", methods=["GET"])
@@ -770,26 +837,27 @@ def lexicon_files():
         for path in root.glob("lexicon*.csv"):
             if path.is_file():
                 files.append(path.name)
                 if len(files) >= 200:
                     break
     except OSError:
         pass
     files.sort()
     return jsonify({"success": True, "files": files})
 
 @app.route("/progress/<scan_id>", methods=["GET"])
 def progress(scan_id):
     if not re.fullmatch(r"[A-Za-z0-9_-]{6,64}", scan_id):
         return jsonify({"success": False, "error": "invalid scan_id"}), 400
     progress_path = PROGRESS_DIR / f"{scan_id}.json"
     if not progress_path.is_file():
         return jsonify({"success": False, "error": "scan_id not found"}), 404
     with open(progress_path, "r", encoding="utf-8") as f:
         data = json.load(f)
     data["success"] = True
     return jsonify(data)
 
 
 if __name__ == "__main__":
     print("Starting Document Scanner Backend Server...")
-    app.run(debug=True, use_reloader=False)
+    port = int(os.environ.get("PORT", "5000"))
+    app.run(debug=True, use_reloader=False, port=port)
diff --git a/index.html b/index.html
index 77352a64c263c2d150a82f29a0d7824a2dc8ccc5..890acd24dd8ec552ffecc2913bc1bad3c6e4d77d 100644
--- a/index.html
+++ b/index.html
@@ -366,135 +366,225 @@
             margin-bottom: 10px;
         }
 
         .match.muted {
             background: #fff6e6;
             border-color: #fcd34d;
         }
 
         .match-title {
             font-weight: 700;
             color: var(--accent-strong);
             font-size: 13px;
             margin-bottom: 6px;
         }
 
         .match small {
             color: var(--ink-muted);
         }
 
         .export {
             display: flex;
             gap: 8px;
             margin-top: 12px;
         }
 
+        .scenario-toggle {
+            display: flex;
+            gap: 12px;
+            flex-wrap: wrap;
+        }
+
+        .scenario-card {
+            border: 1px solid var(--border);
+            border-radius: 12px;
+            padding: 12px 14px;
+            cursor: pointer;
+            background: #f8f4eb;
+            flex: 1;
+            min-width: 220px;
+        }
+
+        .scenario-card.active {
+            border-color: var(--accent);
+            background: #eef9fb;
+        }
+
+        .scenario-card strong {
+            display: block;
+            font-size: 14px;
+            margin-bottom: 4px;
+        }
+
+        .scenario-card span {
+            display: block;
+            font-size: 12px;
+            color: var(--ink-muted);
+            line-height: 1.4;
+        }
+
+        .config-muted {
+            opacity: 0.6;
+            pointer-events: none;
+        }
+
+        .code-block {
+            background: #0f172a;
+            color: #e2e8f0;
+            padding: 12px;
+            border-radius: 10px;
+            font-family: "Courier New", monospace;
+            font-size: 12px;
+            white-space: pre-wrap;
+        }
+
         .curl-preview {
             width: 100%;
             min-height: 120px;
             font-family: "Courier New", monospace;
             font-size: 12px;
             margin-top: 8px;
         }
 
         .hidden {
             display: none;
         }
     </style>
 </head>
 <body>
     <div class="container">
         <header>
             <div>
                 <h1>Document Scanner</h1>
                 <p class="subtitle">OCR-powered scanning with lexicon keywords and regex correlation. Configure inputs, review progress, and export results.</p>
             </div>
             <div id="serverStatus" class="server-pill disconnected">Connecting to backend...</div>
         </header>
 
         <div class="grid">
             <div>
+                <div class="card">
+                    <h2>Scenario</h2>
+                    <div class="info">Select the scenario to expose the right scanning features and outputs.</div>
+                    <div class="scenario-toggle" id="scenarioToggle">
+                        <label class="scenario-card active">
+                            <input type="radio" name="scenarioMode" value="sit" checked hidden>
+                            <strong>Scenario 1: Custom SIT builder</strong>
+                            <span>Scan 1–50 related files to generate a single custom SIT XML and PowerShell import script.</span>
+                        </label>
+                        <label class="scenario-card">
+                            <input type="radio" name="scenarioMode" value="scan_only" hidden>
+                            <strong>Scenario 2: Regex-only scanning</strong>
+                            <span>Scan large datasets using regex patterns only (no keyword correlation).</span>
+                        </label>
+                    </div>
+                </div>
+
                 <div class="card">
                     <h2>Input Source</h2>
                     <div class="input-block">
                         <label for="apiBaseUrl">Backend URL</label>
                         <input type="text" id="apiBaseUrl" value="http://127.0.0.1:5000">
                         <div class="helper">Update if the Flask server runs on a different host or port.</div>
                     </div>
                     <div class="input-block">
                         <label for="inputSource">Select Source</label>
                         <select id="inputSource">
                             <option value="upload">Upload files or folders (browser)</option>
                             <option value="path">Scan a server path</option>
                         </select>
                     </div>
                     <div id="uploadSection" class="input-block">
                         <label>Upload Files / Folders</label>
-                        <div class="info">Supported: PDF, DOCX, XLSX, JPG, PNG, TIFF, EML, MSG</div>
+                        <div class="info" id="fileLimitInfo">Scenario 1 supports 1–50 files. Supported: PDF, DOCX, XLSX, JPG, PNG, TIFF, EML, MSG</div>
                         <div class="file-drop" id="fileDrop">
                             Click to browse or drag files and folders here
                             <small>Folder uploads use the browser picker; drag-drop folders may not work in all browsers.</small>
                             <input type="file" id="fileInput" multiple webkitdirectory directory accept=".pdf,.docx,.xlsx,.jpg,.jpeg,.png,.tiff,.tif,.eml,.msg">
                         </div>
                         <div id="fileList" class="file-list"></div>
                     </div>
                     <div id="pathSection" class="input-block hidden">
                         <label for="scanPath">Server Path</label>
                         <input type="text" id="scanPath" value="" placeholder="/path/to/folder">
                         <div class="helper">Scans files on the server machine instead of uploading.</div>
                     </div>
                 </div>
 
                 <div class="card">
                     <h2>Detection Files</h2>
+                    <div class="info" id="detectionInfo">Scenario 1 uses regex + lexicon keyword correlation.</div>
                     <div class="input-block">
                         <label for="regexPath">Regex Patterns (JSON)</label>
                         <select id="regexPicklist"></select>
                         <input type="text" id="regexPath" value="regex_patterns.json" style="margin-top: 8px;">
                         <div class="helper">Pick a file from the project root, or enter a server path.</div>
                         <div id="regexStatus" class="helper"></div>
                         <div style="margin-top: 8px;">
                             <input type="file" id="regexFile" accept=".json,application/json">
                             <div id="regexFileName" class="helper"></div>
                         </div>
                     </div>
                     <div class="input-block">
                         <label for="lexiconPath">Lexicon Keywords (CSV)</label>
                         <select id="lexiconPicklist"></select>
                         <input type="text" id="lexiconPath" value="lexicon_latest.csv" style="margin-top: 8px;">
                         <div class="helper">Shows CSV files starting with \"lexicon\" in the project root.</div>
                         <div id="lexiconStatus" class="helper"></div>
                         <div style="margin-top: 8px;">
                             <input type="file" id="lexiconFile" accept=".csv,text/csv">
                             <div id="lexiconFileName" class="helper"></div>
                         </div>
                     </div>
                     <button class="btn-secondary" id="refreshLists">Refresh picklists</button>
                 </div>
             </div>
 
             <div>
+                <div class="card" id="sitConfigCard">
+                    <h2>Custom SIT Details</h2>
+                    <div class="info">These values are used to draft the XML rule pack and PowerShell import script.</div>
+                    <div class="input-block">
+                        <label for="sitName">SIT Name</label>
+                        <input type="text" id="sitName" placeholder="Contoso Contracts">
+                    </div>
+                    <div class="input-block">
+                        <label for="sitDescription">SIT Description</label>
+                        <textarea id="sitDescription" rows="3" placeholder="Custom SIT for contract documents."></textarea>
+                    </div>
+                    <div class="row">
+                        <div class="input-block">
+                            <label for="sitPublisher">Publisher</label>
+                            <input type="text" id="sitPublisher" value="Purview Custom">
+                        </div>
+                        <div class="input-block">
+                            <label for="rulePackName">Rule Pack Name</label>
+                            <input type="text" id="rulePackName" value="CustomRulePack">
+                        </div>
+                    </div>
+                </div>
+
                 <div class="card">
                     <h2>Scan Options</h2>
                     <div class="row">
                         <div class="input-block">
                             <label for="fileTypes">File Types (comma-separated)</label>
                             <input type="text" id="fileTypes" value="pdf,docx,xlsx,jpg,jpeg,png,tif,tiff,eml,msg">
                             <div class="helper">Limits scanning to specific extensions.</div>
                         </div>
                         <div class="input-block">
                             <label for="batchSize">Batch Size</label>
                             <input type="number" id="batchSize" value="500" min="1">
                         </div>
                     </div>
                     <div class="row">
                         <div class="input-block">
                             <label for="returnLimit">Return Limit</label>
                             <input type="number" id="returnLimit" value="0" min="0">
                         </div>
                         <div class="input-block">
                             <label for="logPath">Log File Path</label>
                             <input type="text" id="logPath" value="" placeholder="scan.log">
                         </div>
                     </div>
                     <div class="row">
                         <div class="input-block">
@@ -536,114 +626,135 @@
                         <div class="progress-bar" id="progressBar">
                             <span></span>
                         </div>
                         <div class="progress-meta">
                             <span id="progressText">Idle</span>
                             <span id="currentFile"></span>
                         </div>
                     </div>
                     <div class="input-block" style="margin-top: 14px;">
                         <label for="curlPreview">Curl Preview</label>
                         <textarea id="curlPreview" class="curl-preview" readonly></textarea>
                         <div class="buttons" style="margin-top: 8px;">
                             <button class="btn-secondary" id="copyCurl">Copy curl</button>
                         </div>
                     </div>
                 </div>
             </div>
         </div>
 
         <div class="results card" id="resultsSection" style="display: none;">
             <h2>Results</h2>
             <div id="errorMessage" class="message error"></div>
             <div id="successMessage" class="message success"></div>
             <div id="stats" class="stats" style="display: none;"></div>
             <div id="matchesList"></div>
+            <div id="sitOutputs" class="input-block hidden">
+                <h3>Custom SIT Output</h3>
+                <label for="sitXml">Rule Pack XML</label>
+                <pre id="sitXml" class="code-block"></pre>
+                <label for="sitScript" style="margin-top: 12px;">PowerShell Import Script</label>
+                <pre id="sitScript" class="code-block"></pre>
+            </div>
             <div id="exportButtons" class="export" style="display: none;"></div>
         </div>
     </div>
 
     <script>
         const state = {
             files: [],
             results: null,
             serverConnected: false,
             progressTimer: null,
             lastScanId: null
         };
 
         const elements = {
             apiBaseUrl: document.getElementById('apiBaseUrl'),
             inputSource: document.getElementById('inputSource'),
             uploadSection: document.getElementById('uploadSection'),
             pathSection: document.getElementById('pathSection'),
             fileInput: document.getElementById('fileInput'),
             fileDrop: document.getElementById('fileDrop'),
             fileList: document.getElementById('fileList'),
             scanPath: document.getElementById('scanPath'),
             regexPath: document.getElementById('regexPath'),
             lexiconPath: document.getElementById('lexiconPath'),
             regexPicklist: document.getElementById('regexPicklist'),
             lexiconPicklist: document.getElementById('lexiconPicklist'),
             regexFile: document.getElementById('regexFile'),
             lexiconFile: document.getElementById('lexiconFile'),
             regexFileName: document.getElementById('regexFileName'),
             lexiconFileName: document.getElementById('lexiconFileName'),
             regexStatus: document.getElementById('regexStatus'),
             lexiconStatus: document.getElementById('lexiconStatus'),
             refreshLists: document.getElementById('refreshLists'),
+            scenarioToggle: document.getElementById('scenarioToggle'),
+            fileLimitInfo: document.getElementById('fileLimitInfo'),
+            detectionInfo: document.getElementById('detectionInfo'),
+            sitConfigCard: document.getElementById('sitConfigCard'),
+            sitName: document.getElementById('sitName'),
+            sitDescription: document.getElementById('sitDescription'),
+            sitPublisher: document.getElementById('sitPublisher'),
+            rulePackName: document.getElementById('rulePackName'),
             scanBtn: document.getElementById('scanBtn'),
             clearBtn: document.getElementById('clearBtn'),
             progressWrap: document.getElementById('progressWrap'),
             progressBar: document.getElementById('progressBar'),
             progressText: document.getElementById('progressText'),
             currentFile: document.getElementById('currentFile'),
             curlPreview: document.getElementById('curlPreview'),
             copyCurl: document.getElementById('copyCurl')
         };
 
         window.addEventListener('load', () => {
             checkServerConnection();
             loadPicklists();
             updateCurlPreview();
         });
 
         elements.apiBaseUrl.addEventListener('change', () => {
             checkServerConnection();
             loadPicklists();
             updateCurlPreview();
         });
 
         elements.inputSource.addEventListener('change', () => {
             const source = elements.inputSource.value;
             const isUpload = source === 'upload';
             elements.uploadSection.classList.toggle('hidden', !isUpload);
             elements.pathSection.classList.toggle('hidden', isUpload);
             updateScanButtonState();
             updateCurlPreview();
         });
 
+        elements.scenarioToggle.addEventListener('change', () => {
+            updateScenarioUI();
+            updateScanButtonState();
+            updateCurlPreview();
+        });
+
         elements.scanPath.addEventListener('input', () => {
             updateScanButtonState();
             updateCurlPreview();
         });
 
         elements.fileInput.addEventListener('change', handleFileSelect);
         elements.fileDrop.addEventListener('click', () => elements.fileInput.click());
         elements.fileDrop.addEventListener('dragover', handleDragOver);
         elements.fileDrop.addEventListener('dragleave', handleDragLeave);
         elements.fileDrop.addEventListener('drop', handleDrop);
 
         elements.regexFile.addEventListener('change', () => updateOptionalFileName(elements.regexFile, elements.regexFileName));
         elements.lexiconFile.addEventListener('change', () => updateOptionalFileName(elements.lexiconFile, elements.lexiconFileName));
         elements.refreshLists.addEventListener('click', () => loadPicklists(true));
         elements.regexPicklist.addEventListener('change', () => syncPicklistToInput('regex'));
         elements.lexiconPicklist.addEventListener('change', () => syncPicklistToInput('lexicon'));
 
         document.querySelectorAll('input, select, textarea').forEach(el => {
             if (el.id !== 'curlPreview') {
                 el.addEventListener('input', updateCurlPreview);
                 el.addEventListener('change', updateCurlPreview);
             }
         });
 
         elements.regexPath.addEventListener('input', () => {
@@ -789,76 +900,124 @@
             const files = Array.from(e.target.files || []);
             updateFileList(files);
         }
 
         function handleDragOver(e) {
             e.preventDefault();
             elements.fileDrop.classList.add('active');
         }
 
         function handleDragLeave(e) {
             e.preventDefault();
             elements.fileDrop.classList.remove('active');
         }
 
         function handleDrop(e) {
             e.preventDefault();
             elements.fileDrop.classList.remove('active');
             const files = Array.from(e.dataTransfer.files || []);
             const dt = new DataTransfer();
             files.forEach(file => dt.items.add(file));
             elements.fileInput.files = dt.files;
             updateFileList(files);
         }
 
         function updateFileList(files) {
-            state.files = files;
-            if (!files.length) {
+            const mode = getScenarioMode();
+            let normalizedFiles = files;
+            if (mode === 'sit' && files.length > 50) {
+                flashMessage('Scenario 1 supports up to 50 files. Only the first 50 were kept.', 'error');
+                normalizedFiles = files.slice(0, 50);
+                const dt = new DataTransfer();
+                normalizedFiles.forEach(file => dt.items.add(file));
+                elements.fileInput.files = dt.files;
+            }
+
+            state.files = normalizedFiles;
+            if (!state.files.length) {
                 elements.fileList.textContent = '';
             } else {
-                elements.fileList.innerHTML = `<strong>Selected:</strong> ${files.map(f => `${f.name} (${(f.size / 1024 / 1024).toFixed(2)} MB)`).join(', ')}`;
+                elements.fileList.innerHTML = `<strong>Selected:</strong> ${state.files.map(f => `${f.name} (${(f.size / 1024 / 1024).toFixed(2)} MB)`).join(', ')}`;
             }
             updateScanButtonState();
             updateCurlPreview();
         }
 
         function updateOptionalFileName(input, target) {
             if (!input.files || input.files.length === 0) {
                 target.textContent = '';
                 return;
             }
             target.textContent = `Selected: ${input.files[0].name}`;
         }
 
         function updateScanButtonState() {
             const isUpload = elements.inputSource.value === 'upload';
             const hasUpload = state.files.length > 0;
             const hasPath = elements.scanPath.value.trim().length > 0;
             const canScan = isUpload ? hasUpload : hasPath;
             elements.scanBtn.disabled = !state.serverConnected || !canScan;
         }
 
+        function getScenarioMode() {
+            const checked = document.querySelector('input[name="scenarioMode"]:checked');
+            return checked ? checked.value : 'sit';
+        }
+
+        function updateScenarioUI() {
+            const mode = getScenarioMode();
+            const scenarioCards = elements.scenarioToggle.querySelectorAll('.scenario-card');
+            scenarioCards.forEach(card => {
+                const input = card.querySelector('input');
+                card.classList.toggle('active', input && input.checked);
+            });
+
+            const isSit = mode === 'sit';
+            elements.sitConfigCard.classList.toggle('hidden', !isSit);
+            elements.fileLimitInfo.textContent = isSit
+                ? 'Scenario 1 supports 1–50 files. Supported: PDF, DOCX, XLSX, JPG, PNG, TIFF, EML, MSG'
+                : 'Scenario 2 supports large batches. Supported: PDF, DOCX, XLSX, JPG, PNG, TIFF, EML, MSG';
+            elements.detectionInfo.textContent = isSit
+                ? 'Scenario 1 uses regex + lexicon keyword correlation.'
+                : 'Scenario 2 uses regex-only scanning (no keyword correlation).';
+
+            const lexiconInputs = [
+                elements.lexiconPicklist,
+                elements.lexiconPath,
+                elements.lexiconFile,
+                elements.lexiconFileName,
+                document.getElementById('keywordOutputPath')
+            ];
+            lexiconInputs.forEach(input => {
+                if (!input) return;
+                if ('disabled' in input) {
+                    input.disabled = !isSit;
+                }
+                input.classList.toggle('config-muted', !isSit);
+            });
+        }
+
         function generateScanId() {
             if (window.crypto && crypto.randomUUID) {
                 return crypto.randomUUID().replace(/-/g, '').slice(0, 16);
             }
             return `${Date.now()}${Math.floor(Math.random() * 100000)}`.slice(0, 16);
         }
 
         function updateProgressUI(payload) {
             const bar = elements.progressBar;
             const span = bar.querySelector('span');
             const percent = Math.min(100, Math.max(0, payload.percent || 0));
             const isListing = payload.status === 'listing';
             bar.classList.toggle('indeterminate', isListing);
             span.style.width = isListing ? '30%' : `${percent}%`;
             const progressLabel = payload.status === 'complete'
                 ? 'Complete'
                 : payload.status === 'error'
                     ? 'Error'
                     : payload.status === 'listing'
                         ? 'Listing files'
                         : payload.status === 'running'
                             ? 'Scanning'
                             : 'Idle';
             const count = payload.total ? ` (${payload.current}/${payload.total})` : '';
             elements.progressText.textContent = `${progressLabel}${count}`;
@@ -938,146 +1097,172 @@
                         message = 'Upload too large. Use a server path or reduce file sizes.';
                     }
                     throw new Error(message);
                 }
 
                 const results = await response.json();
                 state.results = results;
                 displayResults(results);
                 updateProgressUI({ status: 'complete', percent: 100, current: results.documents_scanned || 0, total: results.documents_scanned || 0 });
             } catch (error) {
                 updateProgressUI({ status: 'error', percent: 0 });
                 showError(error.message || 'Failed to process documents.');
             } finally {
                 stopProgressPolling();
             }
         }
 
         function buildFormData(scanId, isUpload, scanPath) {
             const formData = new FormData();
             if (isUpload) {
                 state.files.forEach(file => formData.append('files', file));
             } else if (scanPath) {
                 formData.append('path', scanPath);
             }
 
+            const scenarioMode = getScenarioMode();
             formData.append('scan_id', scanId);
-            formData.append('lexicon_path', elements.lexiconPath.value.trim() || 'lexicon_latest.csv');
+            formData.append('scenario_mode', scenarioMode);
+            if (scenarioMode === 'sit') {
+                formData.append('lexicon_path', elements.lexiconPath.value.trim() || 'lexicon_latest.csv');
+            }
             formData.append('regex_path', elements.regexPath.value.trim() || 'regex_patterns.json');
 
-            if (elements.lexiconFile.files[0]) {
+            if (scenarioMode === 'sit' && elements.lexiconFile.files[0]) {
                 formData.append('lexicon_file', elements.lexiconFile.files[0]);
             }
             if (elements.regexFile.files[0]) {
                 formData.append('regex_file', elements.regexFile.files[0]);
             }
 
             formData.append('recursive', document.getElementById('recursive').checked);
 
             const fileTypes = document.getElementById('fileTypes').value.trim();
             if (fileTypes) {
                 formData.append('file_types', fileTypes);
             }
 
             formData.append('log_stdout', document.getElementById('logStdout').checked);
 
             const logPath = document.getElementById('logPath').value.trim();
             if (logPath) {
                 formData.append('log_path', logPath);
             }
 
             const debugText = document.getElementById('debugText').checked;
             formData.append('debug_text', debugText);
 
             const debugTextPath = document.getElementById('debugTextPath').value.trim();
             if (debugTextPath) {
                 formData.append('debug_text_path', debugTextPath);
             }
 
             const regexOutputPath = document.getElementById('regexOutputPath').value.trim();
             if (regexOutputPath) {
                 formData.append('regex_output_path', regexOutputPath);
             }
 
-            const keywordOutputPath = document.getElementById('keywordOutputPath').value.trim();
-            if (keywordOutputPath) {
-                formData.append('keyword_output_path', keywordOutputPath);
+            if (scenarioMode === 'sit') {
+                const keywordOutputPath = document.getElementById('keywordOutputPath').value.trim();
+                if (keywordOutputPath) {
+                    formData.append('keyword_output_path', keywordOutputPath);
+                }
             }
 
             const outputPath = document.getElementById('outputPath').value.trim();
             if (outputPath) {
                 formData.append('output_path', outputPath);
             }
 
             const batchSize = document.getElementById('batchSize').value.trim();
             if (batchSize) {
                 formData.append('batch_size', batchSize);
             }
 
             const returnLimit = document.getElementById('returnLimit').value.trim();
             if (returnLimit) {
                 formData.append('return_limit', returnLimit);
             }
 
+            if (scenarioMode === 'sit') {
+                formData.append('sit_name', elements.sitName.value.trim());
+                formData.append('sit_description', elements.sitDescription.value.trim());
+                formData.append('sit_publisher', elements.sitPublisher.value.trim());
+                formData.append('rule_pack_name', elements.rulePackName.value.trim());
+            }
+
             return formData;
         }
 
         function displayResults(results) {
             const resultsSection = document.getElementById('resultsSection');
             const matchesList = document.getElementById('matchesList');
             const stats = document.getElementById('stats');
             const exportButtons = document.getElementById('exportButtons');
+            const sitOutputs = document.getElementById('sitOutputs');
+            const sitXml = document.getElementById('sitXml');
+            const sitScript = document.getElementById('sitScript');
             resultsSection.style.display = 'block';
 
             if (results.errors && results.errors.length > 0) {
                 const errorDiv = document.getElementById('errorMessage');
                 errorDiv.innerHTML = results.errors.join('<br>');
                 errorDiv.style.display = 'block';
             }
 
             const totalDocuments = results.documents_scanned ?? results.total_documents ?? 0;
             const totalMatches = results.total_matches ?? (results.matches ? results.matches.length : 0);
             stats.innerHTML = `
                 <div class="stat"><span>Documents Processed</span><strong>${totalDocuments}</strong></div>
                 <div class="stat"><span>Matches Found</span><strong>${totalMatches}</strong></div>
             `;
             stats.style.display = 'grid';
 
             if (!results.matches || results.matches.length === 0) {
                 matchesList.innerHTML = '<div class="helper">No matches found.</div>';
             } else {
                 matchesList.innerHTML = results.matches.map(match => `
                     <div class="match ${match.rejected ? 'muted' : ''}">
                         <div class="match-title">${escapeHtml(match.type || 'keyword').toUpperCase()}</div>
                         <div><strong>Match:</strong> ${escapeHtml(match.phrase || match.regex_name || match.value || '')}</div>
                         <div><strong>Document:</strong> ${escapeHtml(match.document || '')}</div>
                         ${match.nearest_regex ? `<div><strong>Nearest Regex:</strong> ${escapeHtml(match.nearest_regex)} (${match.nearest_regex_distance ?? 'n/a'})</div>` : ''}
                         ${match.context ? `<small>Context: "${escapeHtml(match.context)}"</small>` : ''}
                     </div>
                 `).join('');
             }
 
+            if (results.sit_outputs && results.sit_outputs.rule_pack_xml) {
+                sitXml.textContent = results.sit_outputs.rule_pack_xml;
+                sitScript.textContent = results.sit_outputs.powershell_script || '';
+                sitOutputs.classList.remove('hidden');
+            } else {
+                sitXml.textContent = '';
+                sitScript.textContent = '';
+                sitOutputs.classList.add('hidden');
+            }
+
             exportButtons.innerHTML = `
                 <button class="btn-primary" onclick="exportResults('json')">Export JSON</button>
                 <button class="btn-secondary" onclick="exportResults('csv')">Export CSV</button>
             `;
             exportButtons.style.display = 'flex';
         }
 
         function showLoading() {
             document.getElementById('resultsSection').style.display = 'block';
             document.getElementById('errorMessage').style.display = 'none';
             document.getElementById('successMessage').style.display = 'none';
             document.getElementById('stats').style.display = 'none';
             document.getElementById('matchesList').innerHTML = '';
             document.getElementById('exportButtons').style.display = 'none';
         }
 
         function showError(message) {
             const errorDiv = document.getElementById('errorMessage');
             errorDiv.textContent = message;
             errorDiv.style.display = 'block';
         }
 
         function flashMessage(message, type) {
             const target = type === 'success' ? document.getElementById('successMessage') : document.getElementById('errorMessage');
             target.textContent = message;
@@ -1103,102 +1288,121 @@
                     const nearestRegexDistance = item.nearest_regex_distance ?? '';
                     const type = item.type || 'keyword';
                     const rejected = item.rejected ?? '';
                     content += `"${String(type).replace(/"/g, '""')}","${String(matchValue).replace(/"/g, '""')}","${String(item.document || '').replace(/"/g, '""')}","${String(context).replace(/"/g, '""')}","${String(nearestRegex).replace(/"/g, '""')}","${String(nearestRegexDistance).replace(/"/g, '""')}","${String(rejected).replace(/"/g, '""')}"\n`;
                 });
                 downloadFile(content, 'results.csv', 'text/csv');
             }
         }
 
         function downloadFile(content, filename, type) {
             const blob = new Blob([content], { type });
             const url = window.URL.createObjectURL(blob);
             const a = document.createElement('a');
             a.href = url;
             a.download = filename;
             document.body.appendChild(a);
             a.click();
             window.URL.revokeObjectURL(url);
             document.body.removeChild(a);
         }
 
         function updateCurlPreview() {
             const apiBase = getApiBaseUrl();
             const isUpload = elements.inputSource.value === 'upload';
             const scanId = state.lastScanId || 'scan1234567890abcd';
+            const scenarioMode = getScenarioMode();
             const lines = [
-                `curl -X POST "${apiBase}/scan" \\\n  -F "scan_id=${scanId}"`
+                `curl -X POST "${apiBase}/scan" \\\n  -F "scan_id=${scanId}" \\\n  -F "scenario_mode=${scenarioMode}"`
             ];
 
             if (isUpload) {
                 if (state.files.length) {
                     state.files.forEach(file => {
                         lines.push(`  -F "files=@/path/to/${file.name}"`);
                     });
                 } else {
                     lines.push('  -F "files=@/path/to/document.pdf"');
                 }
             } else {
                 const scanPath = elements.scanPath.value.trim() || '/path/to/folder';
                 lines.push(`  -F "path=${scanPath}"`);
             }
 
             const addField = (key, value) => {
                 if (value !== '') {
                     lines.push(`  -F "${key}=${value}"`);
                 }
             };
 
-            addField('lexicon_path', elements.lexiconPath.value.trim() || 'lexicon_latest.csv');
+            if (scenarioMode === 'sit') {
+                addField('lexicon_path', elements.lexiconPath.value.trim() || 'lexicon_latest.csv');
+            }
             addField('regex_path', elements.regexPath.value.trim() || 'regex_patterns.json');
             addField('recursive', document.getElementById('recursive').checked);
             addField('file_types', document.getElementById('fileTypes').value.trim());
             addField('log_stdout', document.getElementById('logStdout').checked);
             addField('log_path', document.getElementById('logPath').value.trim());
             addField('debug_text', document.getElementById('debugText').checked);
             addField('debug_text_path', document.getElementById('debugTextPath').value.trim());
             addField('regex_output_path', document.getElementById('regexOutputPath').value.trim());
-            addField('keyword_output_path', document.getElementById('keywordOutputPath').value.trim());
+            if (scenarioMode === 'sit') {
+                addField('keyword_output_path', document.getElementById('keywordOutputPath').value.trim());
+            }
             addField('output_path', document.getElementById('outputPath').value.trim());
             addField('batch_size', document.getElementById('batchSize').value.trim());
             addField('return_limit', document.getElementById('returnLimit').value.trim());
+            if (scenarioMode === 'sit') {
+                addField('sit_name', elements.sitName.value.trim());
+                addField('sit_description', elements.sitDescription.value.trim());
+                addField('sit_publisher', elements.sitPublisher.value.trim());
+                addField('rule_pack_name', elements.rulePackName.value.trim());
+            }
 
             elements.curlPreview.value = lines.join(' \\\n');
         }
 
         function escapeHtml(text) {
             const div = document.createElement('div');
             div.textContent = text;
             return div.innerHTML;
         }
 
         elements.clearBtn.addEventListener('click', () => {
             elements.fileInput.value = '';
             elements.regexFile.value = '';
             elements.lexiconFile.value = '';
             elements.regexPath.value = 'regex_patterns.json';
             elements.lexiconPath.value = 'lexicon_latest.csv';
             elements.scanPath.value = '';
             document.getElementById('fileTypes').value = 'pdf,docx,xlsx,jpg,jpeg,png,tif,tiff,eml,msg';
             document.getElementById('recursive').checked = true;
             document.getElementById('logStdout').checked = true;
             document.getElementById('debugText').checked = false;
             document.getElementById('logPath').value = '';
             document.getElementById('debugTextPath').value = 'extracted_text.log';
             document.getElementById('regexOutputPath').value = 'regex_matches.csv';
             document.getElementById('keywordOutputPath').value = 'keyword_matches.csv';
             document.getElementById('outputPath').value = '';
             document.getElementById('batchSize').value = '500';
             document.getElementById('returnLimit').value = '0';
             state.files = [];
             state.results = null;
             elements.fileList.textContent = '';
             elements.regexFileName.textContent = '';
             elements.lexiconFileName.textContent = '';
+            document.querySelector('input[name="scenarioMode"][value="sit"]').checked = true;
+            elements.sitName.value = '';
+            elements.sitDescription.value = '';
+            elements.sitPublisher.value = 'Purview Custom';
+            elements.rulePackName.value = 'CustomRulePack';
             updateProgressUI({ status: 'idle', percent: 0, current: 0, total: 0, current_document: '' });
             document.getElementById('resultsSection').style.display = 'none';
+            updateScenarioUI();
             updateScanButtonState();
             updateCurlPreview();
         });
+
+        updateScenarioUI();
     </script>
 </body>
 </html>
 
EOF
)